automate experiment setup page (create multiple time slots from the same place) (script)
keyboard shortcuts
additional (Customizeable) fields

frontend time sync (on page load get system time)
    upon loading experiment.html, load the start time of experiment with respect to now
    /data/<int:annotationID>/poll --> return json status and time of start if experiment running

post experiment data - howMuchSolved
    dont show the text field unless they chose False for solved
===========================BUGS==================================
===========================DONE==================================
n number of people

fix per annotator (name when you connect to experiment)
    labeling data should be per annotator

experiment name (display timeslot or some unique name at the top)

type in time for timeslots

export to csv
